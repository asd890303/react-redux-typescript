export default interface MusicInfoModel {
    id: string
    title: string
    author: string
    img_url: string
    length: string
    file_url: string
    use_nums: string
    music_format: string
}